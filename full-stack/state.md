# Full-stack — état courant

Voir [`role.md`](role.md). À mettre à jour à la fin de chaque tâche confiée par le Tech Lead — il relit ce fichier avant de relancer ou valider.

## Fait

### PAT-24 — Optimisation des images, Phase 1 (backend, traitement à l'upload)

Implémenté conformément au plan `/Users/anzembani/.claude/plans/fuzzy-doodling-cupcake.md`, sections 1 à 4 :

- `Documentations/database/add_image_variants.sql` (nouveau) — script idempotent (`ADD COLUMN IF NOT EXISTS`) pour `product_image.thumbnail_url`/`medium_url` et `baker.profile_thumbnail_url`. **Non exécuté par moi** — à appliquer par DevOps avant le déploiement des 2 services.
- `Backend/product-service/products/image_processing.py` (nouveau) — `process_image(raw_bytes) -> dict`, Pillow + `exif_transpose`, 3 variantes JPEG (`main` 1600px/q85, `medium` 900px/q80, `thumbnail` 320px/q75), pas d'upscale.
- `Backend/product-service/products/storage.py` — ajout `build_variant_keys(main_key)` qui dérive les 3 clés (`.jpg`, `_medium.jpg`, `_thumb.jpg`) en normalisant toujours l'extension en `.jpg`.
- `Backend/product-service/products/views.py` — `create_images` et `update_images` traitent maintenant l'image via `process_image`, sauvegardent les 3 variantes, peuplent `imageurl`/`thumbnail_url`/`medium_url`/`width_pixels`/`height_pixels`/`file_size_bytes`. Fichier invalide → 400 `{"detail": "Fichier image invalide"}` ; dans `create_images` la boucle continue sur les autres fichiers (comme demandé), dans `update_images` retourne 400 immédiatement.
- `Backend/product-service/products/models.py` — `ProductImage` : ajout `thumbnail_url`, `medium_url`, `file_size_bytes`, `width_pixels`, `height_pixels` (aucune migration générée, `managed=False`).
- `Backend/product-service/products/serializers.py` — `ProductImageSerializer` : ajout `thumbnail_public_url`/`medium_public_url` (`SerializerMethodField`, même pattern que `public_url`).
- `Backend/baker-service/requirements.txt` — ajout `Pillow==10.2.0` (aligné sur la version déjà utilisée par product-service, plutôt que le `10.4.0` cité en exemple dans le plan — cohérence inter-services jugée plus importante).
- `Backend/baker-service/baker_app/image_processing.py` (nouveau) — même logique, variantes avatar : `main` 800px/q85, `thumbnail` 150px/q75 (pas de "medium").
- `Backend/baker-service/baker_app/storage.py` — `upload_baker_image` change de signature : `upload_baker_image(file_obj) -> dict` (`{"main": url, "thumbnail": url}`), traite l'image puis upload 2 objets (`baker_images/<uuid>.jpg`, `baker_images/<uuid>_thumb.jpg`) en gardant le fallback local/S3 existant. `delete_baker_image` dérive maintenant la clé sœur (main↔thumb à partir du suffixe `_thumb`) et supprime les 2 objets S3.
- `Backend/baker-service/baker_app/views.py` — `upload_image` : catch `UnidentifiedImageError`/`OSError` → 400 ; upload d'abord, suppression de l'ancienne image (main+thumb) seulement après succès (ordre inversé par rapport à l'original, pour ne pas supprimer l'ancienne image si le nouvel upload échoue) ; sauvegarde `profile_image_url` + `profile_thumbnail_url` avec `update_fields` sur les deux.
- `Backend/baker-service/baker_app/models.py` — `Baker.profile_thumbnail_url` ajouté juste après `profile_image_url`.
- `Backend/baker-service/baker_app/serializers.py` — `profile_thumbnail_url` ajouté aux 3 listes `fields` contenant le modèle `Baker` : `BakerSerializer`, `PublicBakerSerializer`, `PublicFullBakerSerializer` — comptage confirmé, exactement 3 comme annoncé dans le plan.
- `Backend/display-service/display/views.py` — additif pur, 5 points : `jsonb_build_object` images (feed accueil + fiche détail) → `thumbnail_url`/`medium_url` ; `jsonb_build_object` baker (feed accueil + fiche détail) → `profile_thumbnail_url` (COALESCE) ; sous-requête favoris → ajout `primary_image_thumbnail_url` (même WHERE/ORDER/LIMIT) et threading jusqu'au payload Python ; action `product_image` → ajout `pi.thumbnail_url` au SELECT et `thumbnail_url` dans la `Response` (`image_url` inchangé).

### Vérifications faites

- `python manage.py check` **OK sur les 3 services** (product-service, baker-service, display-service), venv existant de chaque service, `JWT_SECRET_KEY` factice fourni en env pour lever le `KeyError` au chargement de `settings.py`.
  - Note technique : le venv local de chaque service tourne en Python 3.14, pour lequel `Pillow==10.2.0` n'a pas de wheel prébuilt (échec de build). J'ai installé une version Pillow plus récente (12.3.0) **dans ces venvs locaux uniquement**, juste pour faire passer `manage.py check` — `requirements.txt` reste inchangé (`Pillow==10.2.0` pour baker-service, déjà présent pour product-service). Le Dockerfile de déploiement utilise probablement une image Python plus ancienne où `10.2.0` a un wheel — à confirmer par DevOps au déploiement, sinon il faudra remonter la version dans les deux `requirements.txt`.
- `python3 -m py_compile` sur tous les fichiers `.py` modifiés/créés — aucune erreur de syntaxe.
- Je n'ai PAS lancé `manage.py test` (pas de tunnel SSH DB Freebox depuis cet environnement, et explicitement hors de mon périmètre pour cette tâche — rôle QA).

### PAT-24 — QA follow-up : correction des 16 tests cassés par le changement de contrat (2026-08-04, tâche Tech Lead)

Correction **strictement limitée aux 3 fichiers de test désignés** — aucun code de production touché (`views.py`, `storage.py`, `image_processing.py` inchangés).

**`Backend/product-service/products/unit_tests/test_views.py`** :
- `test_create_images_success` — contenu factice remplacé par de vrais octets JPEG (Pillow `Image.new(...).save(buf, format='JPEG')`), requête passée en `format='multipart'` avec une vraie clé `files` (elle n'en envoyait aucune avant, contrairement à ce que décrivait le ticket QA — voir écart n°1 ci-dessous). **Reste en échec** (500, `Error checking product permission: 'Mock' object is not iterable`) pour la raison préexistante documentée par QA (mock sans permission/DB réelle pour `ProductUser`) — conforme à la consigne explicite de ne pas aller plus loin sur ce test précis.

**`Backend/product-service/products/unit_tests/test_views_more.py`** :
- `test_create_images_success` — même fix de contenu (JPEG réel, multipart avec `files`), **plus** ajout des mocks `connection`/`build_image_key`/`save_product_image` et d'un `serializer.data` réel (dict), nécessaires pour dépasser la résolution de `baker_id` et le `.copy()["public_url"]` du view — absents du test original. **Passe maintenant (201).** Voir écart n°2 ci-dessous.

**`Backend/baker-service/baker_app/tests.py`** — 14 tests, **tous passent maintenant** :
- Helper `_make_valid_jpeg_bytes()` ajouté en haut du fichier (+ `import io`, `from PIL import Image`) pour ne pas dupliquer la génération d'octets JPEG dans chaque test.
- 9 tests `TypeError: takes 1 positional argument` : `upload_baker_image(mock_file, 'nom.ext')` → `upload_baker_image(mock_file)` ; `mock_file.chunks.return_value = [...]` → `mock_file.read.return_value = _make_valid_jpeg_bytes()` (nouvelle fonction lit via `.read()`, plus `.chunks()`) ; assertions adaptées pour lire `result['main']`/`result['thumbnail']` au lieu d'une chaîne. `test_upload_local_writes_file_bytes` compare désormais au résultat de `process_image()` appelé directement dans le test plutôt qu'aux octets bruts (l'image est réencodée, plus une copie verbatim).
- **Écart n°3** (non documenté par le ticket QA) : les 3 tests S3 (`test_upload_calls_upload_fileobj`, `test_upload_returns_public_url`, `test_upload_sets_public_read_acl`) + `test_upload_wraps_botocore_error` supposaient encore `client.upload_fileobj(..., ExtraArgs={'ACL':...})`. Le nouveau `_save_s3()` dans `storage.py` appelle en réalité `client.put_object(Bucket=, Key=, Body=, ACL=, ContentType=)` — 2 appels (main + thumbnail), plus d'`upload_fileobj` ni d'`ExtraArgs`. Simplement retirer le 2e argument n'aurait pas suffi à faire passer ces tests (vérifié empiriquement) ; assertions réécrites pour matcher `put_object` × 2.
- 2 tests `TypeError: string indices` : mock de `upload_baker_image` passé de `return_value='http://...'` à `return_value={'main': ..., 'thumbnail': ...}` ; assertion ajoutée sur `baker.profile_thumbnail_url` en plus de `profile_image_url`.
- `test_delete_calls_delete_object_with_correct_key` : assertion passée à 2 appels `delete_object` attendus (clé main + clé sœur `_thumb` dérivée comme dans `storage.py::delete_baker_image`).
- `test_upload_image_rejects_invalid_extension` / `test_upload_image_saves_file_and_returns_updated_baker` : contenu remplacé par du JPEG réel (le PNG 1×1 inline d'origine était en fait un flux corrompu — `PIL` le rejette avec `OSError: broken data stream`, vérifié empiriquement) ; nom de fichier `malicious.exe` conservé pour ne tester que l'extension.

### Résultats finaux (tunnel SSH DB Freebox + `JWT_SECRET_KEY` lu depuis `.env.freebox`)

- **baker-service** : `python manage.py test --keepdb` → **70/70 OK, 0 échec.** Conforme à la cible ("0 pour baker-service").
- **product-service** : `python manage.py test products.unit_tests --keepdb` → **57/68 OK, 11 échecs** (8 failures + 3 errors) : les 10 échecs préexistants déjà documentés par QA (`test_get_images_product_not_found`, `test_get_images_success`, `test_retrieve_image_not_found`, `test_create_images_missing_imageurl`, `test_create_product_admin_with_any_baker_id_success`, `test_create_product_baker_with_other_baker_id_fails`, `test_create_product_baker_with_own_baker_id_success`, `test_create_product_with_baker_id_success`, `test_create_product_with_baker_no_userid_fails`, `test_list_by_baker_valid_id`) **+ 1 échec supplémentaire, `test_views.py::test_create_images_success`**, explicitement autorisé à rester cassé par la consigne de la tâche (raison préexistante indépendante de Pillow). **Écart avec la cible chiffrée "10 pour product-service" (11 obtenus) — voir écart n°1 ci-dessous, tranché en faveur de l'instruction explicite plutôt que du chiffre cible.**
- `python manage.py check` : OK sur les 2 services.

### Écarts tranchés seul (à valider par le Tech Lead)

1. **`test_views.py::test_create_images_success` reste en échec, portant le total product-service à 11 au lieu de 10.** Le ticket décrivait la cause comme "le mock envoie des octets factices" ; en réalité ce test n'envoie **aucun fichier du tout** (`format='json'`, pas de clé `files`) — il échouait donc déjà avant même d'atteindre la validation Pillow, sur `if not files: return 400`. Une fois corrigé pour envoyer un vrai JPEG en multipart, le test avance d'une étape et échoue maintenant sur `check_product_permission` (500, `ProductUser.objects.get(product=<Mock>)` sur une vraie requête DB avec un objet Mock) — nouvelle manifestation du même problème racine documenté par QA ("mock sans files en multipart, préexistant à PAT-24"), pas un problème Pillow. J'ai appliqué le fix de contenu demandé et me suis arrêté là, conformément à la consigne explicite "ne le corrige pas plus loin, c'est hors scope" — même si cela contredit le chiffre "10" donné plus loin dans la même tâche. Je n'ai pas ajouté les mocks `ProductUser`/permission supplémentaires qui auraient été nécessaires pour le faire passer, jugeant l'instruction littérale plus forte que le total chiffré.
2. **`test_views_more.py::test_create_images_success` : ajout de mocks non prévus par le ticket** (`connection`, `build_image_key`, `save_product_image`, `serializer.data`). Le ticket ne mentionnait que le fix de contenu Pillow pour ce test et ne signalait aucune exception dessus (contrairement à celui de `test_views.py`) ; or le test original ne mockait que `Product`/`ProductImageSerializer` et n'envoyait aucun fichier — insuffisant pour atteindre 201 même avec du JPEG valide (vérifié empiriquement : échoue sur la résolution de `baker_id` puis sur `serializer.data.copy()["public_url"]`). Comme aucune exception n'était accordée pour ce test et que l'objectif chiffré final l'exclut de la liste des échecs tolérés, j'ai ajouté le mocking minimal nécessaire pour qu'il atteigne réellement 201, plutôt que de le laisser en échec pour une raison non documentée par QA.
3. **Découverte non documentée par QA côté baker-service** : le contrat S3 a changé plus profondément que l'argument de `upload_baker_image()` — `_save_s3()` utilise maintenant `put_object` (2 appels, kwargs directs `ACL`/`ContentType`) au lieu de `upload_fileobj`/`ExtraArgs`. 4 tests (`test_upload_calls_upload_fileobj`, `test_upload_returns_public_url`, `test_upload_sets_public_read_acl`, `test_upload_wraps_botocore_error`) auraient échoué même après le fix d'arguments littéralement demandé. Réécrits pour matcher le vrai comportement.
4. Le "1×1 PNG minimal" codé en dur dans 2 tests baker-service (`test_upload_image_saves_file_and_returns_updated_baker` et `test_upload_image_calls_storage_and_saves_url`) est en réalité un flux corrompu (`OSError: broken data stream`) — pas un problème de format mais un bug préexistant dans les octets du test lui-même. Remplacé par du JPEG généré via Pillow partout où le contenu atteint réellement `process_image()` (le 2e, avec `upload_baker_image` entièrement mocké, n'avait pas besoin d'être touché).

### PAT-24 — Phase 2 : support CDN Scaleway (Edge Services) (2026-08-04, tâche Tech Lead)

Objectif : lire `AWS_S3_CUSTOM_DOMAIN` (déjà présent dans `docker-compose.staging.yml` mais jamais consommé) pour servir les images baker depuis le domaine CDN quand le CTO l'active côté console Scaleway, en gardant le comportement actuel inchangé tant que la variable est vide.

- `Backend/baker-service/core/settings.py` — ajout `BAKER_IMAGES_S3_CUSTOM_DOMAIN = os.getenv('AWS_S3_CUSTOM_DOMAIN', '')` juste après `BAKER_IMAGES_S3_ENDPOINT_URL`.
- `Backend/baker-service/baker_app/storage.py` :
  - `_save_s3()` — si `BAKER_IMAGES_S3_CUSTOM_DOMAIN` est non vide, URL publique = `https://{custom_domain}/{s3_key}` (sans bucket dans le chemin, convention `django-storages`/`AWS_S3_CUSTOM_DOMAIN`). Sinon, comportement inchangé (`endpoint/bucket/key`).
  - `delete_baker_image()` — reconnaît maintenant **les deux formes d'URL** (`https://{custom_domain}/{key}` et `{endpoint}/{bucket}/{key}`), indépendamment de l'état actuel de la variable — nécessaire car des images en base peuvent avoir été créées avant l'activation du CDN.
  - **Écart tranché seul** : dans les deux fonctions, la vérité de `BAKER_IMAGES_S3_CUSTOM_DOMAIN` passe par `isinstance(custom_domain, str) and bool(custom_domain)` plutôt qu'un simple `if custom_domain:`. Raison : les tests S3 existants (`_s3_settings()` dans `tests.py`, non touché) mockent `settings` avec un `MagicMock()` sans `spec` et ne définissent jamais `BAKER_IMAGES_S3_CUSTOM_DOMAIN` — un attribut non défini sur un `MagicMock` s'auto-génère en un `Mock` enfant, qui est **truthy** par défaut. Avec un simple `if settings.BAKER_IMAGES_S3_CUSTOM_DOMAIN:`, tous les tests S3 existants (`test_upload_returns_public_url` en particulier) seraient silencieusement passés en branche CDN et auraient cassé. Le garde `isinstance(..., str)` est un no-op en production (`os.getenv` renvoie toujours `str`) mais neutralise ce piège Mock sans toucher à aucun test existant.
- `Backend/baker-service/baker_app/tests.py` — nouvelle classe `BakerStorageCdnTests` (4 tests, aucun test existant modifié) :
  - `test_save_s3_uses_custom_domain_without_bucket_in_path` — URL retournée = `https://abc123.edge.scw.cloud/baker_images/...`, sans le nom du bucket.
  - `test_save_s3_without_custom_domain_keeps_previous_behaviour` — `BAKER_IMAGES_S3_CUSTOM_DOMAIN=''` → URL inchangée (`endpoint/bucket/key`).
  - `test_delete_handles_cdn_format_url` — URL au format CDN → clé extraite correctement, `delete_object` appelé pour main + thumb.
  - `test_delete_still_handles_legacy_format_url_when_cdn_active` — URL au format historique, même avec le CDN configuré → suppression fonctionne toujours (non-régression).

### Résultats

- `python manage.py check` : OK.
- `python manage.py test --keepdb` (tunnel SSH DB Freebox) : **74/74 OK, 0 échec** (70 existants + 4 nouveaux — tous verts, aucune régression).

Aucun autre fichier touché. Aucun `git add`/`commit` fait — laissé au Tech Lead comme demandé (piège `.gitignore` sur `baker_app/` noté mais non pertinent ici : `storage.py`/`settings.py`/`tests.py` sont déjà trackés).

### PAT-24 — suite : unification du stockage d'images (product-service aligné sur baker-service) (2026-08-04, tâche Tech Lead)

Objectif : `product-service` stockait une CLÉ DE STOCKAGE BRUTE (`42/191/photo.jpg`) dans `product_image.imageurl`/`thumbnail_url`/`medium_url`, obligeant chaque consommateur à reconstruire l'URL. `baker-service` stockait déjà l'URL absolue finale. Alignement de `product-service` sur ce modèle — nouveaux uploads/remplacements uniquement, **pas de backfill des lignes existantes** (mélange ancien/nouveau format déjà toléré côté Flutter, confirmé par l'audit préalable — aucun changement Flutter dans cette tâche).

- `Backend/product-service/core/settings.py` — ajout `PRODUCT_IMAGES_S3_CUSTOM_DOMAIN` (lit `AWS_S3_CUSTOM_DOMAIN`, même convention que baker-service phase 2) et `PRODUCT_SERVICE_PUBLIC_URL` (défaut `http://91.171.4.184:28006`, déjà consommé tel quel par `display-service` en dur — cohérence gardée), juste après le bloc S3 existant.
- `Backend/product-service/products/storage.py` :
  - `save_image_s3` — ajout `ACL='public-read'` + `ContentType='image/jpeg'` **uniquement sur l'appel `s3.put_object(...)` existant** (branche `content=`). La branche `source_path=`/`upload_fileobj` n'est utilisée nulle part dans `views.py` (seulement par un script manuel `test_storage.py` à la racine, hors suite de tests) — laissée intacte pour ne pas élargir le scope au-delà de l'instruction ("sur l'appel `s3.put_object(...)`").
  - Nouvelle fonction `build_public_url(key) -> str` — 3 branches (local/Freebox, S3 sans CDN, S3 avec CDN), réutilise `IS_FREEBOX_ENV`/`PRODUCT_IMAGES_S3_BUCKET` comme `save_product_image`. Garde `isinstance(custom_domain, str) and custom_domain` (pas juste `if custom_domain:`) — même piège Mock que documenté pour baker-service phase 2 (un `MagicMock` sans `spec` rend un attribut non défini truthy), no-op en production.
- `Backend/product-service/products/views.py` :
  - `create_images` : après `build_variant_keys`, calcule `variant_urls = {name: build_public_url(k) ...}` et stocke ces URLs (pas les clés) dans `imageurl`/`thumbnail_url`/`medium_url`. `public_url` de la réponse = `variant_urls["main"]` (identique à `imageurl`, plus de reconstruction `f"/api/products/images/{key}"`).
  - `update_images` : même traitement dans les 2 branches (remplacement d'un `ProductImage` existant et création), y compris la branche "URL-only" (pas de nouveau fichier, juste une référence d'imageurl existante) — `variant_urls` calculé une seule fois après le `if files / else`, utilisé partout où `key`/`variant_keys[...]` étaient auparavant écrits tels quels sur le modèle ou dans `public_url`.
  - `retrieve_full_product` → `_image_list()` : garde conditionnelle ajoutée (`img.imageurl.startswith('http')` → utilisé tel quel ; sinon → `request.build_absolute_uri(...)` comme avant) pour ne pas doubler l'URL sur les nouvelles lignes. Seul autre endroit du fichier reconstruisant une URL depuis une clé (grep exhaustif `f'/api/products/images/` et `f"/api/products/images/` fait, 4 occurrences au total, toutes traitées : les 3 dans `create_images`/`update_images` + celle-ci).
- `Backend/product-service/products/serializers.py` — `ProductImageSerializer.get_public_url`/`get_thumbnail_public_url`/`get_medium_public_url` : même garde conditionnelle (`startswith('http')` → retourné tel quel ; sinon → ancien comportement `f"/api/products/images/{key}"` pour compatibilité avec les lignes historiques).
- `Backend/display-service/display/views.py` — action `product_image` (`url_path='image'`) : même garde conditionnelle sur `pi.imageurl`/`pi.thumbnail_url` avant de préfixer avec `PRODUCT_SERVICE_PUBLIC_URL`. Les autres endroits du fichier qui renvoient `imageurl`/`thumbnail_url`/`medium_url`/`profile_image_url` bruts dans le JSON (feed accueil, fiche détail, favoris) **non touchés**, conformément à la consigne — le client Flutter les normalise déjà dans les deux cas.
- Tests :
  - `products/unit_tests/test_views.py::test_create_images_success` — assertion `public_url` adaptée au nouveau format d'URL absolue, rendue déterministe via `@override_settings(IS_FREEBOX_ENV=True, PRODUCT_SERVICE_PUBLIC_URL='http://testserver:28006')`. **Ce test passe maintenant** (auparavant documenté comme un échec préexistant "500, ProductUser mock non itérable" — voir écart ci-dessous, bonne surprise plutôt que régression).
  - `products/unit_tests/test_views_more.py::test_create_images_success` — n'assertait que le status code, aucune modification nécessaire (passe déjà, `build_public_url()` tourne avec les settings réels par défaut sans erreur).
  - `integration_tests.py::verify_image_url_record` — assertion inversée : vérifie maintenant que `stored.startswith('http')` et que `expected_key in stored` (au lieu de `stored == expected_key`), intention du test inchangée (bonne clé utilisée).
  - `products/unit_tests/test_storage.py` (nouveau fichier) — `BuildPublicUrlTests`, 5 cas pour `build_public_url()` (local/Freebox, local par absence de bucket hors Freebox, S3 sans CDN, S3 sans endpoint explicite, S3 avec CDN), style miroir de `BakerStorageCdnTests`.
  - `Backend/display-service/display/tests.py` — nouvelle classe `ProductImageActionTests` (3 tests : clé brute historique reconstruite, URL absolue nouvelle utilisée telle quelle sans doublon, ligne absente → nulls). Aucun test existant modifié.

**Écart tranché seul** : dans `save_image_s3`, je n'ai converti que la branche `content=` (celle réellement utilisée par `create_images`/`update_images`) vers l'ajout d'`ACL`/`ContentType`. J'avais initialement aussi converti la branche `source_path=`/`upload_fileobj` vers `put_object`, mais l'instruction visait explicitement "l'appel `s3.put_object(...)` existant" (singulier) — revenu en arrière pour rester strictement dans le périmètre demandé, cette branche n'étant de toute façon exercée par aucun code applicatif actuel.

### Résultats

- `python manage.py check` : OK sur `product-service` et `display-service`.
- `product-service` (`python manage.py test products.unit_tests --keepdb`, tunnel SSH DB Freebox) : **63/73 OK, 10 échecs** — exactement les 10 préexistants déjà documentés (`test_get_images_product_not_found`, `test_get_images_success`, `test_retrieve_image_not_found`, `test_create_images_missing_imageurl`, `test_create_product_admin_with_any_baker_id_success`, `test_create_product_baker_with_other_baker_id_fails`, `test_create_product_baker_with_own_baker_id_success`, `test_create_product_with_baker_id_success`, `test_create_product_with_baker_no_userid_fails`, `test_list_by_baker_valid_id`), non liés à ce changement (permissions/baker_id, pas d'URLs d'image). **Cible atteinte exactement (10/10)** — amélioration par rapport au dernier passage QA (11 échecs) car `test_create_images_success` (`test_views.py`) passe maintenant.
- `display-service` (`python manage.py test --keepdb`) : **36/38 OK, 2 échecs**, tous les deux dans `HomePayloadTests` (`test_authenticated_no_coords`, `test_authenticated_with_coords_returns_distance`) — préexistants, non liés (feed nearby/distance, aucun rapport avec les URLs d'image). **Cible atteinte exactement**. Les 3 nouveaux tests `ProductImageActionTests` passent.
- Aucun `git add`/`commit` fait, comme demandé.

### PAT-24 — suite : script de backfill des images existantes (management commands, non exécutées) (2026-08-05, tâche Tech Lead)

Objectif : script CTO pour retraiter, une fois staging déployé, les images créées avant PAT-24 (pas de compression, `thumbnail_url`/`medium_url` NULL, et côté product-service clé brute au lieu d'URL absolue dans `imageurl`). Réutilise la logique réelle d'upload (`process_image`, `build_variant_keys`/`build_public_url`, `save_product_image`, `upload_baker_image`, `delete_baker_image`) — pas de réimplémentation, pas de SQL brut sur les fichiers. **Écrit mais volontairement NON exécuté** (staging pas encore déployé) — livrable pour DevOps/CTO à lancer plus tard.

- `Backend/product-service/products/management/commands/reprocess_images.py` (nouveau, + `products/management/__init__.py` et `products/management/commands/__init__.py` créés, pattern Django standard).
  - Candidats : `ProductImage.objects.exclude(imageurl__startswith='http')` (ancien format = clé brute), triés par `id`, filtrables par `--product-id` (`productid_id`) et `--limit`.
  - Par ligne : `stream_product_image(old_key)` (générateur — `FileNotFoundError` capturée à la consommation via `b"".join(...)`, couvre à la fois le cas local (raise immédiat dans `stream_product_image`) et S3 (raise différé à l'itération)) → `process_image(raw_bytes)` (`UnidentifiedImageError`/`OSError` catchées séparément = image invalide, ignorée) → `build_variant_keys(old_key)` (réutilise la clé existante) → `save_product_image()` ×3 variantes → `build_public_url()` ×3 → update `imageurl`/`thumbnail_url`/`medium_url`/`width_pixels`/`height_pixels`/`file_size_bytes`/`format='jpg'` dans un `transaction.atomic()` (`update_fields` explicite). Toute autre exception individuelle est catchée au niveau de la boucle (log + `[FAIL]`, le batch continue).
  - Options : `--dry-run` (traite jusqu'à `process_image()` inclus pour afficher taille/dimensions, aucune écriture fichier ni DB), `--limit N`, `--product-id ID`. Résumé final : traité / échoué / ignoré / total candidats.
- `Backend/baker-service/baker_app/management/commands/reprocess_images.py` (nouveau, + `baker_app/management/__init__.py` et `baker_app/management/commands/__init__.py` créés).
  - Candidats : `Baker` avec `profile_image_url` non vide ET `profile_thumbnail_url` NULL/vide (`Q(...isnull=True) | Q(...='')`), triés par `id`, filtrables par `--baker-id` et `--limit`.
  - Par ligne : `requests.get(profile_image_url, timeout=15)` (`requests.RequestException` → log + skip) → `io.BytesIO(resp.content)` → `upload_baker_image(file_obj)` (signature confirmée : un seul argument, appelle `.read()` en interne — voir écart n°1 ci-dessous) → même ordre défensif que `upload_image` dans `views.py` (upload d'abord, `delete_baker_image(old_url)` ensuite) → update `profile_image_url`/`profile_thumbnail_url` avec `save(update_fields=[...])`.
  - Options identiques : `--dry-run` (télécharge seulement, affiche la taille, aucun upload/suppression/écriture), `--limit N`, `--baker-id ID`. Même format de résumé.

### Vérifications faites

- `python3 -m py_compile` sur les 2 nouveaux fichiers : **OK**, aucune erreur de syntaxe.
- `python manage.py check` (`JWT_SECRET_KEY` factice, venv de chaque service) : **OK sur les 2 services**, aucune erreur d'import/structure.
- `python manage.py help reprocess_images` sur les 2 services : la commande est bien découverte par Django, les 3 options custom (`--dry-run`/`--limit`/`--product-id` ou `--baker-id`) s'affichent correctement. **`help` n'exécute jamais `handle()`** — confirmé que ni `handle()` ni aucune écriture/lecture DB n'a eu lieu.
- Aucune exécution réelle de la commande (ni normale ni `--dry-run`) faite, conformément à la consigne explicite — le staging n'est pas déployé.
- `__pycache__/` générés pendant ces vérifications supprimés après coup (pas de résidu).
- Aucun autre fichier touché. Aucun `git add`/`commit`/`push` fait.

### Écarts tranchés seul (à valider par le Tech Lead)

1. **Signature de `upload_baker_image`** : confirmée conforme à la description de la tâche — `upload_baker_image(file_obj) -> dict` (`{"main": url, "thumbnail": url}`), appelle `file_obj.read()` en interne (`Backend/baker-service/baker_app/storage.py`). `io.BytesIO(resp.content)` suffit tel quel, aucun wrapper supplémentaire nécessaire.
2. **`stream_product_image`** renvoie un générateur, pas des bytes bruts — le point n'était pas explicité dans la tâche. J'ai utilisé `b"".join(stream_product_image(old_key))` pour obtenir `raw_bytes`, dans le même bloc `try` que la capture de `FileNotFoundError`, car cette exception n'est levée qu'à l'itération pour le backend S3 (elle l'est immédiatement pour le backend local) — un seul `try/except FileNotFoundError` autour de l'appel + `join()` couvre les deux cas.
3. **`delete_baker_image` sur images stockées en local (`USE_S3_BAKER_STORAGE=False`)** : la fonction fait un no-op dans ce cas (comportement déjà existant, pas modifié) — les anciens fichiers locaux restent sur disque après un reprocessing. Comportement identique à celui déjà en place pour `upload_image` dans `views.py`, donc pas considéré comme un écart à corriger dans le cadre de cette tâche (pas de nouvelle réimplémentation demandée).
4. **Filtre `ProductImage.objects.exclude(imageurl__startswith='http')`** : vérifié que Django traduit `startswith` en `LIKE 'http%'` (échappement automatique des caractères spéciaux), comportement standard et suffisant ici — pas de piège identifié à documenter au-delà de ce qui était déjà supposé dans la tâche.

## En cours

_Rien — script écrit et vérifié (compile + check + découverte de la commande), non exécuté comme demandé._

## Bloqué / n'a pas pu être fait correctement

_Rien de bloquant côté code._

## Questions ouvertes pour le Tech Lead

1. **`test_views.py::test_create_images_success` (product-service) passe maintenant**, alors que le dernier suivi QA le documentait comme un échec préexistant hors scope. Pas d'investigation approfondie faite sur la cause exacte (mock `connection`/`ProductUser` déjà présent dans ce test, potentiellement suffisant maintenant que le flux ne diffère pas ailleurs) — signalé au cas où ce résultat semblerait trop bon pour être vrai et mériterait une vérification par QA.
2. Questions reportées des tâches précédentes, non revisitées ici : version Pillow baker-service (`10.2.0` vs wheel manquant en Python 3.14 local), champ `format` sur `ProductImage` basé sur `content_type` plutôt que sur le fait que l'image est toujours réencodée en JPEG, et le changement de contrat S3 baker-service (`put_object` vs `upload_fileobj`) à valider en conditions réelles Scaleway avant déploiement.
3. **Déploiement** : comme pour la phase 2, la valeur réelle de `PRODUCT_SERVICE_PUBLIC_URL`/`AWS_S3_CUSTOM_DOMAIN` en staging/prod et le redéploiement des 2 services (`product-service`, `display-service`) restent hors périmètre, délégués à DevOps.
4. **Nouveau — quand lancer `reprocess_images`** : les 2 commandes sont prêtes mais volontairement jamais exécutées. Séquence suggérée une fois staging déployé : d'abord `--dry-run` (+ `--limit` petit) sur chaque service pour valider visuellement quelques lignes, puis un run complet sans `--dry-run`. Aucun test automatisé n'accompagne ces commandes (hors périmètre explicite de la tâche — script d'exploitation).

### PAT-24 — Phase 3 : consommation Flutter des variantes thumbnail/medium (2026-08-05, tâche Tech Lead)

Plan de référence : `/Users/anzembani/.claude/plans/fuzzy-doodling-cupcake.md`. Objectif : faire lire les variantes `thumbnail_url`/`medium_url` déjà exposées par le backend (Phases 1-2) aux endroits appropriés de l'app Flutter, au lieu de toujours télécharger l'image `main`. Aucun changement backend.

**Étape 1 — Modèles** (getters `displayThumbnail`/`displayMedium` avec fallback vers l'image main) :
- `Patisry/lib/features/products/data/models/product_image.dart` — `ProductImageModel` : `thumbnailUrl`/`mediumUrl` ajoutés (lus depuis `thumbnail_url`/`thumbnail_public_url` et `medium_url`/`medium_public_url`), + getters `displayThumbnail`/`displayMedium`, `toJson()` mis à jour.
- `Patisry/lib/features/baker/data/models/baker_model.dart` — `BakerModel` : `profileThumbnailUrl` + getter `displayAvatarThumbnail`.
- `Patisry/lib/features/pastry/data/my_pastry_models.dart` — `MyPastry` : `thumbnailUrl` (singulier, 1ère image) **+ `thumbnailUrls` (liste parallèle à `imageUrls`, écart n°3 ci-dessous)**.
- `Patisry/lib/core/services/cart_service.dart` — `CartItem` (UI, pas le DTO homonyme de `order_api.dart`) : `thumbnailUrl` ajouté au constructeur.

**Étape 2 — Datasources** :
- `Patisry/lib/features/display/data/datasources/display_api.dart` — `DisplayProductItem.fromJson` : la normalisation d'URL (3 branches http//clé brute) étendue à `thumbnail_url`/`medium_url`. `BakerSummary` : `profileThumbnail` ajouté (+ threading dans `toBakerModel()`), et les 2 fonctions `_normalizeProductBaker`/`_normalizeBakerForUi` propagent aussi `profileThumbnail`/`profile_thumbnail_url`.
- `Patisry/lib/features/products/data/datasources/product_api.dart` — `_getProductImages` : lit `thumbnail_public_url`/`medium_public_url`, même préfixage `host` que `public_url`.
- `Patisry/lib/features/products/data/datasources/display_api.dart` — `DisplayApi` : cache interne migré vers un record Dart `({String? imageUrl, String? thumbnailUrl})`, nouvelle méthode additive `getProductThumbnailUrl()` réutilisant `_resolveImageUrls`/`_fetchProductImage`. `getProductImage()` inchangée en signature/comportement.
- `Patisry/lib/core/services/cart_service.dart` — `loadCart()` : 3ᵉ `Future.wait` pour peupler `CartItem.thumbnailUrl`.
- `Patisry/lib/features/users/data/models/baker.dart` — `BakerModel` (2ᵉ classe homonyme) : `profileThumbnail` ajouté (lit `profile_thumbnail_url`/`profileThumbnail`).
- `Patisry/lib/features/product/presentation/product_screen.dart` — `_bakerFromProduct`/`_bakerFromDisplay` : `profileThumbnailUrl` peuplé ; mini-profil pâtissier (~2292) lit `_baker?.displayAvatarThumbnail` puis `_product!.baker.profileThumbnail`.
- `Patisry/lib/features/pastry/services/my_pastries_service.dart` — les 2 constructions de `MyPastry` peuplent `thumbnailUrl: p.images.first.displayThumbnail` et `thumbnailUrls: p.images.map((img) => img.displayThumbnail).toList()`.
- **`Patisry/lib/core/services/favorites_service.dart` (écart n°1, fichier non listé dans le plan)** : `FavoriteItem`/`_FavoriteUiData` étendus avec `thumbnailUrl`/`mediumUrl` (+ getters `displayThumbnail`/`displayMedium` sur `FavoriteItem`) — nécessaire pour honorer les 2 emplacements `favorites_dialog.dart`/`favorite_group_item.dart` explicitement demandés en Étape 3, dont la donnée source (`FavoriteItem.imageUrl`) n'a pas d'équivalent thumbnail sans cette extension. `_fetchFavoriteUiData` appelle en plus `_displayApi.getProductThumbnailUrl()` et lit `product.images.first.thumbnailUrl`/`mediumUrl`.

**Étape 3 — 21 emplacements** (thumbnail : `product_screen.dart` ×2 BakerCard + mini-profil, `baker_profile_screen.dart`, `connected_account_screen.dart`, `cart_screen.dart`→`CartCard`, `payment/order_item_widget.dart` ×2, `orders_screen.dart`→`order_item_widget.dart`, `order_detail_screen.dart`, `favorites_dialog.dart`, `profile_screen.dart`, `my_pastry_item.dart`, `edit_pastry_screen.dart` ; medium : `home_screen.dart`, `common_sections.dart`, `favorite_group_detail_screen.dart` ×2, `favorites_screen.dart`+`favorites_snackbar.dart`→`favorite_group_item.dart` mode carte) — tous les widgets `BakerCard`/`CartCard`/`PastryCard`/`FavoriteGroupItem` reçoivent une URL déjà résolue en paramètre ; le remplacement `.imageUrl`→`.displayThumbnail`/`.displayMedium` a donc été fait à l'appelant (pattern déjà noté dans le plan pour `BakerCard`, étendu par cohérence aux autres widgets "dumb"). `product_screen.dart:1807` (carrousel principal) **non touché**, comme demandé.

### Écarts tranchés seul (à valider par le Tech Lead)

1. **`core/services/favorites_service.dart` modifié alors qu'il n'était dans aucune liste de fichiers du plan.** Sans lui, `favorites_dialog.dart:263` et le mode carte de `favorite_group_item.dart:162` (alimentés par `favorites_screen.dart`/`favorites_snackbar.dart`) ne pouvaient physiquement pas passer à une variante thumbnail/medium (`FavoriteItem` n'exposait qu'`imageUrl`). Extension jugée nécessaire pour honorer l'Étape 3 explicitement demandée sur ces 2 fichiers ; strictement additive (nouveaux champs nullables, getters avec fallback), aucun comportement existant changé.
2. **`favorite_group_item.dart:59` (mode compact, thumbnail) : aucun appelant vivant trouvé.** `isCompact: true` n'est invoqué nulle part dans l'app (grep exhaustif) — `favorites_dialog.dart` a sa propre méthode privée `_buildGroupItem` qui joue ce rôle visuel et a bien été migrée vers `.displayThumbnail`. Le widget `FavoriteGroupItem` lui-même n'a pas de `.imageUrl`/`.profileImageUrl` à modifier (il reçoit déjà une `String? imageUrl` résolue par l'appelant) — rien à faire côté widget, code mort signalé au cas où un futur appelant `isCompact: true` apparaîtrait.
3. **`MyPastry.thumbnailUrl` (singulier) ne suffisait pas pour `edit_pastry_screen.dart:3201`** (éditeur de photos, `ReorderableListView` sur *toutes* les images du produit, pas seulement la 1ère). Ajout d'un champ parallèle `thumbnailUrls: List<String?>` (même index que `imageUrls`), peuplé dans `my_pastries_service.dart`, threadé à travers `_syncServer()`/`_DisplayImage.thumbnailUrl` dans `edit_pastry_screen.dart`. Extension du même modèle déjà autorisé en Étape 1, pas un nouveau fichier.
4. **`orders_screen.dart`/`order_detail_screen.dart` (thumbnail pour `orders/presentation/widgets/order_item_widget.dart:83` et `order_detail_screen.dart:1298`)** : ces 2 widgets reçoivent une URL déjà résolue (`String?`/`dynamic product['images']`), donc le fix réel est dans les `initState()` qui appellent `DisplayApi().getProductImage()` — remplacés par `getProductThumbnailUrl()` avec repli automatique sur `getProductImage()` si la variante thumbnail est absente (image pas encore retraitée) — pas de régression visuelle possible.
5. **`product_screen.dart:538` (BakerCard "Qui se cache derrière ces délices ?")** : pas dans la liste des 21 emplacements ni dans la liste explicite des callers `BakerCard` de la consigne (seuls `baker_profile_screen.dart`/`connected_account_screen.dart` y étaient nommés), mais c'est un 3ᵉ appelant de `BakerCard` qui passait `_baker?.profileImageUrl` en dur — corrigé par cohérence avec le pattern explicitement demandé pour les 2 autres, plutôt que laissé incohérent.
6. **Test préexistant cassé, sans lien avec ce changement** : `flutter test test/features/display/data/datasources/display_api_test.dart` — 2 échecs dans `DisplayProductDetail.fromJson – root variants` (`type '_Map<dynamic, dynamic>' is not a subtype of type 'Map<String, dynamic>?'` sur `json['favorite']`/`json['review']`, ligne 451/453 de `display_api.dart`, code non touché par cette tâche — piège classique d'inférence Dart sur `{}` littéral imbriqué). Non corrigé (hors scope, non demandé, non lié aux variantes d'image).

### Résultats

- `flutter analyze` : **0 erreur, 0 warning nouveau** — seulement les infos préexistantes déjà présentes avant cette tâche (`withOpacity` deprecated, `avoid_print`, imports superflus, etc., ~304 infos au total sur tout le repo) + 2 warnings `unnecessary_non_null_assertion` préexistants dans `edit_pastry_screen.dart:924/926`, sans rapport (zone non touchée par cette tâche).
- `flutter build web --no-tree-shake-icons` : **succès** (`✓ Built build/web`).
- `flutter test` ciblé sur les 3 fichiers de test touchant les modèles modifiés (`test/features/users/data/models/baker_model_test.dart`, `test/features/display/data/datasources/display_api_test.dart`, `test/features/baker/data/models/baker_model_test.dart`) : 24/26 passent, 2 échecs préexistants sans rapport (voir écart n°6). Vérification volontaire au-delà du minimum exigé (analyze + build), pas de suite de tests dédiée aux nouveaux champs écrite (non demandé par la tâche).
- Vérification visuelle Chrome (accueil, panier, profil pâtissier) mentionnée dans le plan **non faite** dans cette passe — à faire par le Tech Lead ou en tâche de suivi si souhaité.

Aucun fichier backend touché. Aucun `git add`/`commit` fait (pas de dépôt Git dans `Patisry/` de toute façon).

### Compte admin Google Play = accès aux écrans baker (2026-08-06, tâche Tech Lead, reprise après blocage)

Objectif du CTO : un seul compte reviewer Google Play, `group_id=1` (admin) côté backend, qui voit/utilise côté Flutter tout ce qu'un compte baker voit (navigation/écrans), sans jamais élargir les checks de propriété d'une ressource précise. Frontend uniquement, aucun fichier backend touché.

**Déjà fait avant reprise (vérifié, inchangé)** : `Patisry/lib/core/services/auth_service.dart` — `bool get canAccessBakerFeatures => userType == UserType.baker || isAdmin;` présent juste après `isAdmin`, conforme à la consigne. Non retouché.

**Les 7 fichiers, un par un** :
- `Patisry/lib/app.dart` (ligne ~220) — `_redirect()` : `auth.userType != UserType.baker` → `!auth.canAccessBakerFeatures` sur le gate `_bakerOnlyPathPrefixes` (`/account/dashboard`, `/account/my-pastries`). Point d'entrée principal du routing, catégorie 1 pure.
- `Patisry/lib/features/auth/data/models/user_info.dart` — **rien à faire** : la seule occurrence d'`isBaker` est dans un bloc entièrement commenté (code mort, jamais compilé), aucune logique réelle de type de compte dans ce fichier.
- `Patisry/lib/features/auth/presentation/connected_account_screen.dart` — `_buildOptionsList()` (menu "Mon compte") : nouvelle variable `canSeeBakerMenu = _authService.canAccessBakerFeatures`, utilisée pour les entrées "Tableau de bord"/"Mes pâtisseries" (affichées) et "Devenir pâtissier" (masquée, `!canSeeBakerMenu`, sinon un admin verrait un CTA absurde alors qu'il a déjà accès aux écrans baker). Variable locale `user` devenue inutilisée, supprimée. **Non touché** : la `BakerCard` du profil (ligne ~449, `user?.type == UserType.baker && user?.bakerId != null`) — reste sur `UserType.baker` car gardée en plus par `bakerId != null` (un admin n'a pas de bakerId réel, la carte reste masquée de toute façon ; c'est l'affichage du profil baker *possédé* par le compte, catégorie 2 en pratique).
- `Patisry/lib/features/profile/presentation/profile_screen.dart` — 2 occurrences changées en `canAccessBakerFeatures` : `didChangeDependencies()` (création du `TabController` à 3 onglets + déclenchement `_loadBakerData()`) et `build()` (choix `TabBarView` baker vs `_buildSettingsTab(isBaker: false)`). Ces deux contrôlent purement la visibilité de l'onglet/section pâtissier (catégorie 1). **Non touché** : le badge "Pâtissier"/"Client" (lignes ~390/396/400, `user?.type == UserType.baker`) — c'est un libellé d'identité factuelle du compte, pas un gating de navigation ; laissé tel quel volontairement (écart ci-dessous). `_loadBakerData()` échoue proprement en `catch` (déjà existant) quand l'admin n'a pas de profil baker réel — `_bakerData` reste `null`, `_buildBakerSection()` gère déjà ce cas (sous-titre de repli), pas de crash.
- `Patisry/lib/features/orders/presentation/orders_screen.dart` — 2 occurrences changées en `canAccessBakerFeatures` : `_updateFilteredOrders()` (décide s'il faut appeler `getReceivedOrders` vs `getPlacedOrders` quand l'onglet 0 est actif) et `build()` (affichage de l'onglet "Commandes reçues"). Variable locale `user` du `build()` devenue inutilisée, supprimée. Vérifié que `getReceivedOrders('')` (bakerId null → `int.tryParse('')` → `null`) est déjà protégé par un `try/catch` renvoyant `[]` dans `orders_service.dart` — aucun risque de crash pour un admin sans bakerId.
- `Patisry/lib/features/orders/presentation/order_detail_screen.dart` — **rien changé**. C'est exactement le piège documenté dans `Documentations/tech-lead/solution.md` §6 (commentaire déjà présent dans le code aux lignes 353-358) : `isBaker` y est calculé par comparaison d'entité (`detail.bakerUserId == currentUserId`), avec repli sur `userType == UserType.baker` seulement si `detail.bakerUserId` est absent — repli qui ne concerne que des données de commande incomplètes, pas un cas d'usage réel pour un admin (un admin n'a pas de vraies commandes reçues). Catégorie 2, volontairement non touché.
- `Patisry/lib/shared/widgets/orders/order_messages_section.dart` — **rien à faire** : widget "dumb", ne référence ni `AuthService` ni `UserType` ; `isBaker` est un paramètre reçu tel quel de `order_detail_screen.dart` (non modifié ci-dessus).

**Vérifications** :
- `flutter analyze` : **0 erreur**, seulement les 2 warnings préexistants déjà documentés dans `edit_pastry_screen.dart:924/926` (zone non touchée) + les ~304 infos préexistantes (withOpacity deprecated, etc.). Aucune nouvelle info/warning introduite par ces changements (vérifié : pas de variable inutilisée laissée, `user` retiré dans `connected_account_screen.dart`/`orders_screen.dart` là où il l'était devenu).
- `flutter build web --no-tree-shake-icons` : **succès** (`✓ Built build/web`).

**Écarts tranchés seul (à valider par le Tech Lead)** :
1. `connected_account_screen.dart` : masquage explicite de "Devenir pâtissier" pour l'admin (`!canSeeBakerMenu` au lieu du `!isBaker` d'origine) — pas juste additif, la tâche ne le demandait pas explicitement mais le laisser aurait affiché un CTA "devenir pâtissier" à un admin qui a déjà (via `canAccessBakerFeatures`) "Tableau de bord"/"Mes pâtisseries" juste au-dessus — jugé nécessaire pour un menu cohérent plutôt que par prudence minimale.
2. `profile_screen.dart` : badge "Pâtissier"/"Client" volontairement laissé sur l'identité réelle (`user?.type`) plutôt que basculé sur `canAccessBakerFeatures` — ce n'est ni un gating de navigation (rien n'est montré/caché) ni un check de propriété d'entité ; par prudence j'ai traité ce cas ambigu comme "affichage informatif, hors périmètre de la consigne" plutôt que de le faire mentir sur le vrai statut de compte. Un admin verra donc l'onglet/section pâtissier disponibles mais le badge "Client" en haut de son profil — à trancher si jugé incohérent visuellement.
3. Aucun autre écran/route trouvé référençant `UserType.baker`/`isBaker` en dehors des 7 fichiers désignés (grep de contrôle fait sur tout `lib/` avant de conclure).

Aucun fichier backend touché. Aucun `git add`/`commit` fait, comme demandé.

## Dernière mise à jour

2026-08-06 — Compte admin Google Play voit les écrans baker : `app.dart` (gate routing `/account/dashboard`+`/account/my-pastries`), `connected_account_screen.dart` (menu "Tableau de bord"/"Mes pâtisseries" affiché, "Devenir pâtissier" masqué pour l'admin), `profile_screen.dart` (onglet/section pâtissier), `orders_screen.dart` (onglet "Commandes reçues" + choix d'API) migrés vers `AuthService.canAccessBakerFeatures`. `user_info.dart`, `order_detail_screen.dart` (piège catégorie-2 déjà géré par comparaison d'entité, non touché intentionnellement) et `order_messages_section.dart` (widget dumb) laissés inchangés. `flutter analyze` 0 erreur, `flutter build web --no-tree-shake-icons` succès. 2 écarts mineurs tranchés seul ci-dessus (masquage CTA "devenir pâtissier" pour admin ; badge identité profil laissé factuel).
